<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para Mapbox y ubicación -->
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>







    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SupervisorApp">

        <!-- Token de acceso de Mapbox -->
        <meta-data
            android:name="com.mapbox.accessToken"
            android:value="pk.eyJ1IjoiZnJlZGR5ZmllcnJvIiwiYSI6ImNtMzk2eHFtYzExbGcyam9tZG8yN3d2aXQifQ.Yx7HsOnTVplMFrFJXMRYSw" />

        <!-- MainActivity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SupervisorApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- MapActivity -->
        <activity
            android:name=".activities.MapActivity"
            android:exported="false"
            android:label="Mapa de Tablets"
            android:theme="@style/Theme.SupervisorApp"/>
        <!-- AlertsActivity si la usas -->
        <activity
            android:name=".activities.AlertsActivity"
            android:exported="false"
            android:label="Alertas"
            android:theme="@style/Theme.SupervisorApp"/>
        <activity android:name=".activities.TabletDetailActivity" />
    </application>
</manifest>
